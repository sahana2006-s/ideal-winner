N, M = map(int, input().split())
# Top pattern
for i in range(1, N, 2):
    print(('.|.' * i).center(M, '-'))
# Center line
print('WELCOME'.center(M, '-'))
# Bottom pattern
for i in range(N-2, -1, -2):
    print(('.|.' * i).center(M, '-'))
